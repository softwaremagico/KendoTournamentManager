Replace the content of the `docker` folder with these files and execute:

```
docker-compose build && docker-compose up -d
```

For accessing to the tool, please open a browser and access to:

```
http://localhost:4200
```