name: Sonar Backend
on:
  push:
    paths:
      - 'backend/**'
      - '.github/workflows/**'
    branches:
      - main
      - master
  pull_request:
    paths:
      - 'backend/**'
      - '.github/workflows/**'
    types:
      - opened
      - synchronize
      - reopened



jobs:
  build:
    name: Build and Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Disabling shallow clones is recommended for improving the relevancy of reporting
          fetch-depth: 0
      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: maven
      - name: Build with Maven and generate coverage
        run: mvn -B clean verify -Dgpg.skip=true -Djacoco.skip=false --file backend/pom.xml
      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@master
        with:
          projectBaseDir: backend
          args: >
            -Dsonar.organization=softwaremagico
            "-Dsonar.projectName=Kendo Tournament Manager"
            -Dsonar.projectKey=kendo-tournament-backend
            -Dsonar.java.binaries=kendo-tournament-logger/target/classes,kendo-tournament-persistence/target/classes,kendo-tournament-core/target/classes,kendo-tournament-pdf/target/classes,kendo-tournament-rest/target/classes
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco-aggregate/jacoco.xml
            -Dsonar.verbose=true
            -Dsonar.test.exclusions=**/test/*
            -Dsonar.exclusions=kendo-tournament-persistence/src/test,kendo-tournament-core/src/test,kendo-tournament-pdf/src/test,kendo-tournament-rest/src/test
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}